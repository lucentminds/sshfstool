{
	"name": "sshfstool",
	"date": "10-09-2017",
	"description": "Handy tool kit for use with [sshfs](https://github.com/libfuse/sshfs)..",
	"author": {
		"name": "Scott Johnson"
	},
	"license": "UNLICENSED",
	"version": "0.0.1",
	"engines" : {
		"node" : ">=4.0.0"
	},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"bin": {
		"": "Determines the command that will be globally available : Determines the script that will act as the global app.",
		"sshfstool": "bin/sshfstool.js"
	},

  	"keywords": [],
	"files": [
		"bin",
		"config-example.json"
	],

	"repository": {
		"type" : "git",
		"url" : "https://github.com/npm/npm.git"
	},

	"dependencies": {}
}
